{
  "description": "Load the no-ai-prose rules into context at the start of every session",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/load-skill.sh",
            "args": [],
            "timeout": 5,
            "statusMessage": "Loading no-ai-prose rules"
          }
        ]
      }
    ]
  }
}
